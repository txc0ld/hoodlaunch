import { useState } from 'react';
import { createFundingPlan } from '../lib/exchange-plan';
import styles from './ProAccess.module.css';
export default function ExchangeFunding({ addresses }: { addresses: readonly string[] }) {
 const [amount,setAmount]=useState('0.01');const [error,setError]=useState('');
 function download(){try{const plan=createFundingPlan(addresses,amount);const url=URL.createObjectURL(new Blob([JSON.stringify(plan,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download=`hoodlabs-ethereum-funding-${plan.id}.json`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);setError('');}catch(e){setError(e instanceof Error?e.message:'Unable to create the funding plan.');}}
 return <section className={styles.panel} aria-labelledby="exchange-funding-title"><p className={styles.label}>SOURCE FUNDING · ETHEREUM MAINNET</p><h2 id="exchange-funding-title">Fund from your exchange</h2><p>Prepare an Ethereum ETH withdrawal plan for these {addresses.length} verified wallet addresses, then use the local exchange companion or your exchange’s withdrawal screen.</p><div className={styles.controls}><label>Withdrawal request per wallet (ETH)<input inputMode="decimal" value={amount} onChange={e=>setAmount(e.target.value)} aria-describedby="exchange-fees" /></label><button type="button" onClick={download}>Download Ethereum funding plan</button></div><p id="exchange-fees">Exchange fees can change the net amount received or the total debited. Leave additional Ethereum ETH for bridge gas. This plan contains addresses and amounts only; it does not submit a withdrawal.</p><p>Keep API keys on your own computer. <a href="/exchanges" target="_blank" rel="noopener noreferrer">Set up Binance, Kraken or OKX →</a></p>{error&&<p role="alert">{error}</p>}</section>;
}
