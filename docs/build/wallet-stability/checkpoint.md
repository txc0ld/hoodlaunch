# Current checkpoint

The active task follows the shipped Hoodlabs revision 55ba878. The user reports Max Sell All failing after Max Buy All and intermittent refreshes, and requests automatic completion/holdings updates, a manual refresh-all control, and stable prompt submission. Standing authorization includes shipping after the required checks. No real funds or owner secrets are included. See contract.md.

The candidate is /home/tx/projects/hoodlabs-wallet-stability on fix/wallet-transaction-stability. Remote main remains the base 55ba878. Financial changes are integrated at 76a4018, copied from critical owner 14e50a1. The next commit 2dfc570 records the contract. Only docs are currently being clarified; no further production source has changed in the candidate.

The critical owner reproduced insufficient exit gas after Max Buy and a 50-node batch stopping at the old shared quote expiry. The approved reserve and bounded per-node renewal are implemented. A delayed actual-RPC fixture completed 50/50 submissions in 141.454 seconds, versus 24/50 before. This measures reliable completion under delay, not faster confirmation. Core checks passed; the deliberate long test ran separately. Evidence: /home/tx/projects/hoodlabs-max-trades/artifacts/wallet-stability/candidate-evidence.md. Independent verification of the core passed at the exact 76a4018 source hashes, including 61 targeted checks and 8 additional boundary/race checks. Changes to the account guards will require affected core verification again.

The Sol High UI owner is working in /home/tx/projects/hoodlabs-wallet-status-ui. A real component fixture reproduced missing automatic status refresh, missing refresh-all, and a forced scroll of 2775 pixels without navigation. Approved work includes a coalesced read cycle with concurrency three, per-hash/session/token guards, incremental status updates during other submissions, holdings refresh with retries, a native-balance callback, and removal of forced focus/scroll. The browser and typecheck gates now pass, including previous-token transaction recovery, manual refresh during a batch, partial/slow RPC failure handling, stale-hash fencing, and holdings retry after the last pending transaction completes. The UI commit cca910d passed the initial browser checks. Root inspection identified stale original batch results potentially replacing early confirmations, timeout accounting for unresolved reads, and manual recovery without a loaded token snapshot. The UI owner retains exclusive ownership for these bounded repairs before integration.

The independent verifier is at /home/tx/projects/hoodlabs-wallet-stability-verify. Account tests show that an in-deadline delayed status request preserves the workspace. Two transient failures reproduce wallet loss and stop later writes while preserving the submitted node's record. This is now a failing acceptance case under the account-outage addendum. Focus/visibility are stable; pagehide revokes signing. The exact cause on the user's device remains unconfirmed.

Next steps are to finish the UI slice, combine it with the financial changes, freeze the candidate, run the required full tests/typecheck/build/scanner/audit/browser checks, obtain independent verification and fresh Astra High review, then run exact-commit hosted CI and deploy with live acceptance. New evidence belongs in artifacts/wallet-stability. Runtime model, token usage and cost telemetry are unknown. This followup has not been released yet.

Scope correction: the independent double-transient-failure reproduction is part of the continuing wallet-loss complaint. The separate R3 preflight is approved at /home/tx/projects/hoodlabs-wallet-account-recovery. The proposed behavior retains the same owner's wallet but centrally suspends every financial action until fresh account verification succeeds. Real revocation/logout/pagehide/idle still forget. The critical owner is implementing the approved central account gate and owner/action generations. Four additional actual-vault red tests demonstrate missing direct-access and pause/recovery guards. These original failures now pass in the critical worktree. Its expanded browser matrix passes outage retention, coalescing, fresh recovery, fatal/identity/logout cleanup and email code sign-in. Seven preliminary independent API tests also pass; none is final combined-candidate evidence yet. UI ownership transfers after the UI commit; the account owner then adds readiness props without changing the workspace key. Exact preflight evidence is in that worktree at artifacts/wallet-stability/account-preflight.md. This supersedes the earlier decision to leave all account failure behavior unchanged. See contract addendum.
